import type { VideoProject } from '../types/portfolio';

// TODO: replace with real video projects (tapes). Keep videoSrc lazy-loaded —
// see lib/performance.ts guidance, never preload all tapes at once.
export const videos: VideoProject[] = [];
