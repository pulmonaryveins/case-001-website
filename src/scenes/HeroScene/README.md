# HeroScene

Hybrid investigation board with a lazy Three.js environment and accessible DOM evidence. See root ARCHITECTURE.md for rendering ownership, camera registration and performance rules; ASSETS.md records provisional models and final material slots.

Environment components live in environment/. HeroScene preserves case, subject, discipline, note and string composition. Mobile/WebGL failure retains a DOM board. GSAP owns the entrance; no Hero-to-Desk transition or later scene is implemented.
